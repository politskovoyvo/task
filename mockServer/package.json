{
  "name": "mockserver",
  "version": "1.0.0",
  "description": "server to angular mock",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "task",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
