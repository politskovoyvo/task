export type SessionStorageKeys = 'refreshToken' | 'access_token';

export const SESSION_STORAGE_KEYS = {
  refreshToken: 'refreshToken' as SessionStorageKeys,
  access_token: 'access_token' as SessionStorageKeys,
};
